<Window x:Class="iVillager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:iVillager"
        mc:Ignorable="d"
        Title="iVillager"
        Height="899"
        Width="420"
        MinWidth="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_PreviewKeyDown"
        Background="#0B1320">

    <Window.Resources>
        <!-- Nordic palette -->
        <SolidColorBrush x:Key="Bg" Color="#0B1320"/>
        <SolidColorBrush x:Key="Surface" Color="#111C2E"/>
        <SolidColorBrush x:Key="Surface2" Color="#0F192A"/>
        <SolidColorBrush x:Key="Border" Color="#24324A"/>

        <SolidColorBrush x:Key="Text" Color="#EAF2FF"/>
        <SolidColorBrush x:Key="Subtext" Color="#A8B6CC"/>

        <SolidColorBrush x:Key="Primary" Color="#2A9D8F"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#35B7A6"/>
        <SolidColorBrush x:Key="PrimaryPressed" Color="#1E7E73"/>

        <SolidColorBrush x:Key="WarnBg" Color="#2A1F16"/>
        <SolidColorBrush x:Key="WarnBorder" Color="#C18A53"/>
        <SolidColorBrush x:Key="WarnText" Color="#FFD7A3"/>

        <CornerRadius x:Key="CardRadius">16</CornerRadius>

        <!-- Typography -->
        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="BodyText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Subtext}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="LineHeight" Value="18"/>
        </Style>

        <!-- Card -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource Surface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{StaticResource CardRadius}"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.35"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryHover}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryPressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource Surface2}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#16253C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0F1C2E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox -->
        <Style x:Key="NordicTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="{StaticResource Surface2}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Checkbox -->
        <Style x:Key="NordicCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

    </Window.Resources>

    <Grid Background="{StaticResource Bg}">
        <!-- subtle background glow -->
        <Ellipse Width="520" Height="520" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-220,-220,0" Opacity="0.18">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#2A9D8F" Offset="0"/>
                    <GradientStop Color="#0B1320" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <Border Width="380"
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        Margin="0,28,0,0"
        Background="#0B1320"
        BorderBrush="#18243A"
        BorderThickness="1"
        CornerRadius="22"
        Padding="18" Height="817">

            <!-- Scroll gdy okno jest niższe -->
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="4,4,4,14">
                        <TextBlock Text="iVillager" Style="{StaticResource TitleText}"/>
                        <TextBlock Style="{StaticResource BodyText}" Margin="0,6,0,0"
                           Text="Minimalny panel sterowania. Ustaw region kolejki, potem Start."/>
                    </StackPanel>

                    <!-- Warning -->
                    <Border Grid.Row="1"
                    CornerRadius="{StaticResource CardRadius}"
                    Padding="16"
                    Background="{StaticResource WarnBg}"
                    BorderBrush="{StaticResource WarnBorder}"
                    BorderThickness="1"
                    Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock Foreground="{StaticResource WarnText}" FontSize="14" FontWeight="SemiBold"
                               Text="Wymagane: Global Build Queue"/>
                            <TextBlock Foreground="{StaticResource WarnText}" Margin="0,8,0,0" FontSize="13" LineHeight="18" TextWrapping="Wrap"
                               Text="W opcjach gry włącz „Globalną Kolejkę Budowy / Global Build Queue”. Potem w overlay zaznacz jej obszar na ekranie." />
                        </StackPanel>
                    </Border>

                    <!-- Main content -->
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Start/Stop card -->
                        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,14">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Sterowanie" Style="{StaticResource SectionTitle}"/>
                                <TextBlock Grid.Row="1" Style="{StaticResource BodyText}" Margin="0,8,0,14"
                                   Text="Kliknij Start, gdy region jest zapisany. Stop zatrzymuje wykrywanie."/>
                                <Button Grid.Row="2"
                                x:Name="StartStopButton"
                                Content="Start"
                                Click="StartStopButton_Click"
                                Style="{StaticResource PrimaryButton}"/>

                                <TextBlock x:Name="DetectedIconText"
                                   Grid.Row="2"
                                   Text=""
                                   Foreground="#8DE6DC"
                                   FontSize="12"
                                   Margin="0,54,0,0"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Top"
                                   Visibility="Collapsed"/>
                            </Grid>
                        </Border>

                        <!-- Overlay + Start/Stop hotkey -->
                        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Overlay" Style="{StaticResource SectionTitle}"/>

                                <TextBlock Grid.Row="1" Style="{StaticResource BodyText}" Margin="0,8,0,10"
                                   Text="Overlay służy tylko do zaznaczenia regionu Global Build Queue. Skrót do overlay jest stały: Ctrl + Page Up." />

                                <CheckBox Grid.Row="2"
                                  x:Name="OverlayHotkeyToggle"
                                  Style="{StaticResource NordicCheckBox}"
                                  Content="Włącz skrót overlay (Ctrl + Page Up)"
                                  Margin="0,0,0,14"
                                  Checked="OverlayHotkeyToggle_Changed"
                                  Unchecked="OverlayHotkeyToggle_Changed"/>

                                <TextBlock Grid.Row="3" Text="Skrót Start/Stop" Style="{StaticResource SectionTitle}"/>

                                <TextBlock Grid.Row="4" Style="{StaticResource BodyText}" Margin="0,8,0,12"
                                   Text="Ten skrót uruchamia Start/Stop globalnie (działa też, gdy gra ma fokus)." />

                                <TextBox Grid.Row="5"
                                 x:Name="HotkeyInput"
                                 Style="{StaticResource NordicTextBox}"
                                 ToolTip="np. Ctrl+Shift+F1"
                                 Margin="0,0,0,12"/>

                                <Grid Grid.Row="6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0"
                                    x:Name="RebindHotkeyButton"
                                    Content="Rebind"
                                    Click="RebindHotkeyButton_Click"
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="0,0,10,0"/>

                                    <Button Grid.Column="1"
                                    Content="Zapisz"
                                    Click="SaveHotkeyButton_Click"
                                    Style="{StaticResource SecondaryButton}"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Footer -->
                    <TextBlock Grid.Row="3"
                       Style="{StaticResource BodyText}"
                       Foreground="#7F8EAA"
                       TextAlignment="Center"
                       Margin="6,14,6,2">
                <Run Text="Jeśli coś nie działa: odpal overlay i ponownie zaznacz region kolejki."/>
                    </TextBlock>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
